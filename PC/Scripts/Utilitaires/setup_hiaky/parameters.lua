--- <parameters>

-- Temps d'attente maximal de la connexion du bot kamas (en secondes)
maxWaitingTime = 120

-- Temps de déconnexion avant de réessayer de connecter le bot kamas (en heures)
timeToRetry = 4

-- Temps d'attente entre chaque checkup du script controller (en secondes)
checkinTime = 30

-- Kamas que le bot bank donnera
neededAmount = 1200000

-- Alias du bot qui donnera les kamas /!\ sans le serveur -> exemple : "giver_1" ou "giver"  /!\
giverAlias = "bank"

-- true : active les print du script
-- false : désactive
verbose = true


shushiIDs = { 
    18154, -- coiffe
    --18155, -- cape

}

mountIDs = { 
    7810, -- ébène/ivoire
    7808, -- ébène
}


--- </parameters>